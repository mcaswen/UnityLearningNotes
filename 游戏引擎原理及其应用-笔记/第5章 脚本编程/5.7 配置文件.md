#### 一、`Json`
- `JavaScript Object Notation`
- 一种轻量级的数据交换格式
- `Json`语法是`JavaScript`对象表示法的子集
- `Json`键/值队由键和值组成

#### 二、`JsonUtility`
- 通过`FromJson`将`Json`转换为`Object`
- 通过`ToJson`将一个`Object`对象序列化为一个`Json`文件