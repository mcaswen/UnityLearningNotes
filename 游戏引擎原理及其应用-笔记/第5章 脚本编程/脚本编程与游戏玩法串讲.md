### 一、`C#`语言/`Unity`集成高级特性
#### 1) `Lambda`表达式

- **作用**：

- **使用方法/应用**：

- **原理**：

- **注意事项**：
	- 建议不要在`Lambda`表达式中包含复杂逻辑
	- 如有需要可以提取成一个成员方法，并赋予有意义的命名

#### 2) 协程 `Coroutine`

- **作用**：

- **使用方法/应用（以`MapManager`为例）**：

- **原理**：

- **注意事项**：
	- `Coroutine`的生命周期是整个方法的执行生命周期，而非游戏对象的生命周期
	- 在对象的生命周期结束（如调用`Destroy()`时），要手动`StopCoroutine()`，使当前可能正在运行的协程停止。



#### 3) List 列表 数据结构

- **作用**：

- **使用方法/应用（以Timer为例）**：

- **原理**：

- **注意事项**：

#### 4) Dictionary 字典/键值对 数据结构

- **作用**：

- **使用方法/应用（以Timer为例）：

- **原理**：

- **注意事项**：

#### 5) 委托与事件

- **作用**：

- **使用方法/应用**：

- **原理**：

- **注意事项**：

### 二、游戏开发常用设计模式

#### 1) 单例模式

- **作用**：

- **使用方法/应用**：

- **原理**：

- **注意事项**：

#### 2) 观察者模式与事件中心

- **作用**：

- **使用方法/应用**：

- **原理**：

- **注意事项**：

### 三、开发中注意事项

#### 1) 架构图设计

#### 2) 一些小tips

- 善用缓存，避免每帧调用`GameObject.Find()` / `GameObject.FindWithTag()` 等高开销方法

- 命名规范，赋予方法和变量有意义的命名，并统一公有/私有变量的命名标准

- 灵活使用日志，如`Debug.Log()`, `Debug.LogWarning()`来明晰游戏运行中开发者所需的信息或警告，或做功能的`Debug`调试

- 利用`IDE`提供的强大搜索功能来阅读一个模块的代码或调用栈

- 明确“一类一对象”的面向对象程序设计原则，不要设计出一个巨类（如将输入，移动，摄像机，动画，行为逻辑(`Ani`分配等)全部写在一个`Player.cs`脚本中）


